# Repo Skills

仓库内的 prompt/agent 说明已收敛到 `skills/`。

- `skills/a-share-analysis/`：A 股公司研究与中文报告输出。
- `skills/deepmoat-local-data/`：读取项目数据库和本地 HTTP 接口，作为外部 `tushare-data` skill 的补充。

`agents/` 目录现在主要保留知识库原始资料；新的行为入口请优先使用 `skills/`。
